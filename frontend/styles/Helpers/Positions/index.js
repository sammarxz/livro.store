import { css } from 'styled-components'

const Positions = css`
  .p--relative {
    position: relative;
  }

  .p--fixed {
    position: fixed;
  }

  .p--absolute {
    position: absolute;
  }
`

export { Positions }